#ifndef PACKET_PRIORITISER_H
#define PACKET_PRIORITISER_H

#include "../common/Packet.h"
#include <vector>

// Declaration of the prioritisePackets function
std::vector<Packet> prioritisePackets(const std::vector<Packet>& packets);

#endif // PACKET_PRIORITISER_H

